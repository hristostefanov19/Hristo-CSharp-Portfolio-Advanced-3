<Window x:Class="ExpenseTracker.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Expense Tracker" Height="450" Width="400">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,10">
            <TextBox x:Name="txtDescription" Width="120" Height="25"
                     Text="Description"
                     GotFocus="txtDescription_GotFocus"
                     LostFocus="txtDescription_LostFocus" Margin="0,0,5,0"/>
            <TextBox x:Name="txtAmount" Width="80" Height="25"
                     Text="Amount"
                     GotFocus="txtAmount_GotFocus"
                     LostFocus="txtAmount_LostFocus" Margin="0,0,5,0"/>
            <DatePicker x:Name="dpDate" Width="120"/>
            <Button Content="Add" Width="50" Margin="5,0,0,0" Click="BtnAdd_Click"/>
        </StackPanel>

        <ListBox x:Name="lstExpenses" Grid.Row="1" DisplayMemberPath="Display" Margin="0,0,0,10"/>

        <StackPanel Orientation="Horizontal" Grid.Row="2" HorizontalAlignment="Center">
            <Button Content="Delete" Width="60" Margin="5" Click="BtnDelete_Click"/>
        </StackPanel>
    </Grid>
</Window>

